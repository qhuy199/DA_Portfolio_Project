/DA_Portfolio_Project
├── /data                # Dữ liệu thô (Input)
│   ├── /raw             # Các file CSV gốc (không bao giờ sửa)
│   └── /processed       # Các file tạm (nếu có)
│
├── /sql                 # Tầng xử lý dữ liệu (Transformation Layer)
│   ├── 01_staging.sql   # Tạo các bảng gốc (DDL)
│   ├── 02_views.sql     # Các VIEW sạch (vw_asset_clean, vw_dim_employees)
│   └── 03_audit.sql     # Các VIEW kiểm tra lỗi (vw_asset_audit)
│
├── /scripts             # Mã nguồn Python
│   ├── load_data.py     # Script đẩy dữ liệu từ CSV -> PostgreSQL
│   ├── audit_check.py   # Script báo cáo sức khỏe (như chúng ta đã viết)
│   └── utils.py         # Các hàm dùng chung (db connection)
│
├── /reports             # File Power BI hoặc tài liệu phân tích
│   └── IT_Asset_Dashboard.pbix
│
├── .gitignore           # Quan trọng: Không upload file mật, file venv lên Git
├── README.md            # Tài liệu dự án (Mô tả dự án, cách chạy, kết quả)
└── requirements.txt     # Danh sách thư viện (psycopg2, pandas,...)